<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

    <style name="LoginTheme1.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="LoginTheme1.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!-- inherit from the material theme -->
    <style name="WebTheme" parent="Theme.AppCompat.Light.NoActionBar" />

</resources>